<!doctype html>
<html lang="ru" ng-app="partyApp">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">

    <title>Let's go to the Party!</title>

  </head>

  <body>

  {{{body}}}


  <style >

  body {
    background-color: fafafa;
  }

  fieldset {
    border-radius: 3px;
    border: 1px solid #666;
    background-color: #fff;
  }

  .form-wrapper {
    padding: 10px;
  }

  @media (min-width: 800px) {
    .form-wrapper {
      max-width: 750px;
      margin: 0 auto;
    }
  }

  fieldset + fieldset {
    margin-top: 40px;
  }

  .form-input-wrapper {
    display: flex;
    flex-direction: column-reverse;
  }

  .form-input-wrapper label {
    color: 888;
    font-size: 15px;
    margin-bottom: 5px;
  }

  @media (min-width: 800px) {
    .form-input-wrapper {
      display: flex;
      flex-direction: row-reverse;
      align-items: center;
    }
  }

  input, placeholder, select {
    color: #999;
  }

  input, select{
    min-height: 34px;
    padding: 0px;
    font-size: 17px;
    border: 1px solid #ebebeb;
    outline: none;
    transition: transform .4s, box-shadow .4s, background-position .2s;
    background:  #f1f1f1;
    position: relative;
  }



  .form-input-wrapper input, select {
    flex: 0 0 50%;
    border: 1px solid #ccc;
    color: #222;
  }

  .form-input-wrapper label {
    flex: 1 0 50%;
    font-size: 15px;

  }

  .form-input-wrapper + .form-input-wrapper {
    margin-top: 10px;
  }

  legend {
    font-size: 1.3em;
    padding: 3px 6px;
    color: #FFF;
    margin-bottom: 10px;
    border-radius: 2px;
    background-color: #739327;
  }

  .form-input-wrapper input:focus, {
    border: 1px solid #236a7e;
    transform: scale(1.03);
    z-index: 3;
  }

  label:after {
    content: "*";
    font-size: 1.1em;
    position: relative;
    display: inline-flex;
    margin-left: .2ch;
    transition: color, 1s;
    color: red;
  }

  input:required:valid + label:after, select:required:valid + label:after  {
    color: green;
  }

  .form-submit-btn {
    padding: 0;
    margin: 1em 0;
    font-size: 1rem;
    position: relative;
    width: 300px;
    height: 44px;
  }

  </style>


  </body>
</html>
